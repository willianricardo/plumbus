import 'package:flutter/material.dart';

class AppColors {
  // primary
  static const primary = Color(0XFFFA8100);
  static const primaryContrast = Color(0XFFFFFFFF);
  static const primaryShade = Color(0XFFDC7200);
  static const primaryTint = Color(0XFFFB8E1A);

  // secondary
  static const secondary = Color(0XFF3DC2FF);
  static const secondaryContrast = Color(0XFFFFFFFF);
  static const secondaryShade = Color(0XFF36ABE0);
  static const secondaryTint = Color(0XFF50C8FF);

  // tertiary
  static const tertiary = Color(0XFF5260FF);
  static const tertiaryContrast = Color(0XFFFFFFFF);
  static const tertiaryShade = Color(0XFF4854E0);
  static const tertiaryTint = Color(0XFF6370FF);

  // success
  static const success = Color(0XFF2DD36F);
  static const successContrast = Color(0XFFFFFFFF);
  static const successShade = Color(0XFF28BA62);
  static const successTint = Color(0XFF42D77D);

  // warning
  static const warning = Color(0XFFFFC409);
  static const warningContrast = Color(0XFF000000);
  static const warningShade = Color(0XFFE0AC08);
  static const warningTint = Color(0XFFFFCA22);

  // danger
  static const danger = Color(0XFFEB445A);
  static const dangerContrast = Color(0XFFFFFFFF);
  static const dangerShade = Color(0XFFCF3C4F);
  static const dangerTint = Color(0XFFED576B);

  // dark
  static const dark = Color(0XFF222428);
  static const darkContrast = Color(0XFFFFFFFF);
  static const darkShade = Color(0XFF1E2023);
  static const darkTint = Color(0XFF383A3E);

  // medium
  static const medium = Color(0XFF92949C);
  static const mediumContrast = Color(0XFFFFFFFF);
  static const mediumShade = Color(0XFF808289);
  static const mediumTint = Color(0XFF9D9FA6);

  // light
  static const light = Color(0XFFF4F5F8);
  static const lightContrast = Color(0XFF000000);
  static const lightShade = Color(0XFFD7D8DA);
  static const lightTint = Color(0XFFF5F6F9);

  // component colors
  static const background = Color(0XFFEEEEEE);
  static const input = Color(0XFFFFFFFF);
  static const favorite = Color(0XFFF44336);

  // colors for the app
  static const white = Colors.white;
  static const black = Colors.black;
  static const blue = Colors.blue;
}
